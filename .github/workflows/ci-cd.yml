name: CI/CD RanchDuBonheur

on:
  push:
    branches: [master]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v2 # Pour la compatibilité multi-architecture
        
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2 # Configuration de Buildx
        
      - name: Build and push Docker image (multi-arch)
        uses: docker/build-push-action@v3 
        with:
          context: .
          platforms: linux/amd64,linux/arm64  # Spécifiez les architectures cibles
          push: true
          tags: ${{ secrets.DOCKER_USERNAME }}/ranchdubonheur:latest
          # Optionally, build with a specific Dockerfile
          file: ./Dockerfile

      - name: Deploy to VPS
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USERNAME }}
          key: ${{ secrets.VPS_SSH_PRIVATE_KEY }}
          script: |
            cd /var/www/ranchdubonheur
            docker-compose down
            docker pull ${{ secrets.DOCKER_USERNAME }}/ranchdubonheur:latest
            docker-compose up -d
